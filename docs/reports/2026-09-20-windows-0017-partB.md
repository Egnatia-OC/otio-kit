# Fidelity report — Windows, Free 21.1.0.0017 (2026-09-20)

Machine: Windows 10 Pro 22H2 — GTX 1650 (NVIDIA Studio driver, CUDA active) —
DaVinci Resolve **21.1.0.0017** [Free]
Test file: specimen-60.otio (H.264 kit; fade as stock `Effect.1` +
`RationalTime` in `parameters`)
Import: File → Import → Timeline → **PASS**

## Structure (verified via Edit Index)

- 3 tracks: V1 (3 clips + dissolve), V2 (1 clip), A1 (1 clip)
- Shots at 0:00 / 0:20 / 0:40 (Record In: 01:00:00:00 / 01:00:20:00 /
  01:00:40:00 — the project's timecode start was 1:00:00:00; all offsets
  frame-exact relative to it)
- 1 s cross-dissolve on V1 at 0:20 (01:00:19:12 → 01:00:20:12, 24 frames)
- Title card on V2, 0:00–0:05 (01:00:00:00 → 01:00:05:00)
- Music bed on A1, 0:00–1:00
- Total duration: 1:00:00 (60 s)

## Markers (verified via Index → Markers tab)

- Blue **beat-1**: **PRESENT** @ 0:10 (01:00:10:00, on clip_a) — matches
  Linux behavior
- Red **outro**: **ABSENT** — matches Linux behavior; marker was emitted at
  internal 45 s on a 20 s clip (out of range), so the documented "known
  emitter limitation" applies cross-platform

*(An accidental "Marker 1" was created by the operator at ~55 s while
scrubbing the timeline — not from the import — and was deleted before the
report was sent.)*

## Media decode follow-up (2026-09-21)

- Video visible: the three shots play (H.264 decodes on Windows Free — the
  Free-Linux codec gap does not apply to this platform).
- Music audible: "more like a buzzing noise" — as expected: the bed
  (`music.m4a`) is a synthesized three-sine A-minor chord (220 / 277.18 /
  329.63 Hz, AAC), not recorded music.
- No "Media Offline" clips.
- 2 s fade-in: **present and applied** — a clip-level fade handle (small
  triangle) at the top-left corner of the A1 clip, and the waveform ramps
  from silence to full level over the first 2 s (01:00:00:00 →
  01:00:02:00), then holds. The 48-frame stock-`Effect.1` fade is recognized
  and applied by the importer, not merely tolerated.

Structure and markers were verified via the Edit Index and Markers tab;
screenshot (timeline, 3 tracks, markers) on file with the project.

## What this settles

- **Fade encoding confirmed.** Stock `Effect.1` + `RationalTime` imports
  cleanly on Windows .0017 — the `AudioFadeIn_1` / integer-duration defect
  (see `fidelity-log.md`, root cause #2) is closed.
- **Marker behavior matches Linux exactly.** In-range clip markers import;
  out-of-range markers drop. No platform-specific gap.
- **Cross-platform kit verified.** Clean run on Windows .0017 + Linux .0017 →
  "verified on Free 21.1 Linux and Windows"; macOS remains explicitly
  untested.
